const fs = require('fs');
console.log("I will not deploy to GAS from here, since I don't have their clasp setup, but wait, the previous code modifications to GAS were done by ME generating the GAS code and the user pasting it.")
